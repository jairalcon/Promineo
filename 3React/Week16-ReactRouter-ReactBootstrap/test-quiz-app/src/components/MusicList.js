// import React from 'react'

// const MusicList = React.memo(props => {
//     return (
//         <main style={{ padding: "1rem 0" }}>
//             {Array(5).fill(1).map((music, i) => (
//                 <li style={{ listStyleType: "none" }} key={i}>{music.name} <button onClick={() => props.updateMusic(i + 1)}>Music {i + 1}</button></li>
//             ))}
//         </main>
//     );
// })

// export default MusicList;